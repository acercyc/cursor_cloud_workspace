# AGENTS.md

## Skills

| Skill | File | When to use |
|---|---|---|
| New Task Folder | `.cursor/skills/new-task-folder.md` | Every time the user asks for a completely new task (not a follow-up to a previous one). Creates a dedicated folder at the repo root for all files related to that task. |

## Cursor Cloud specific instructions

This repository is currently a blank scaffold with no application code. It contains only a `README.md` and a `.gitignore` (which hints at future Node.js and/or Python usage).

- **No services to run.** There are no applications, backends, frontends, or workers.
- **No dependencies to install.** There is no `package.json`, `requirements.txt`, `pyproject.toml`, or any other dependency manifest.
- **No lint, test, or build commands.** No tooling is configured yet.

When application code is added, update this file with relevant setup, run, lint, test, and build instructions.
